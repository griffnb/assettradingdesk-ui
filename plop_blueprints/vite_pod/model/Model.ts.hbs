import { attr } from "@/models/decorators/attr";
import { findConstant } from "@/models/constants";
import { ValidationRules } from "@/utils/validations";
import { constants } from "../constants";
import { {{pascalCase name}}BaseModel } from "./{{pascalCase name}}BaseModel";
import { validationRules } from "./validation_rules";
import { IStore } from "@/models/types/store";
import { ParentInfo } from "@/ui/common/components/types/bread-crumb";


export class {{pascalCase name}}Model extends {{pascalCase name}}BaseModel {
  _model_name: StoreKeys = "{{lowerCase name}}";
  get validationRules(): ValidationRules {
      return validationRules;
  }


    // Search Result Values
  get label(): string {
    return `${this.name} `;
  }

  get icon(): string {
    return "fa fa-user";
  }

  get link(): string {
    return `/{{lowerCase pluralName}}/details/${this.id}`;
  }

  getParent(): ParentInfo|null {
    /*
    return {
      model: "",
      id: `${this.family_id}`,
    };
    */
    return null
  }

  getBreadCrumb(){ //parent: any) {
    /*
    return {
      title: parent ? parent.name : this.family_primary_account_name,
      url: `/families/details/${this.family_id}`,
    };
    */
    return {
      title: "",
      url: "",
    }
  }

  constructor(store: IStore<{{pascalCase name}}Model>) {
    super(store);
    this.addObserve(this);
  }

}
