import { useAuth } from "@/common_lib/authentication/useAuth";
import { {{pascalCase name}}Index }  from "@/admin/pods/{{lowerCase name}}/pages/{{pascalCase name}}Index";
import { observer } from "mobx-react";

export default observer(() => {
  const { isAuthenticated, authLoading } = useAuth();
  if (authLoading) {
    return null
  }

  if (!isAuthenticated) {
    return null;
  }

  return <{{pascalCase name}}Index />;
});
