import { AssetManagementIndex } from "@/customer/pods/assets/management/AssetManagementIndex";
import { observer } from "mobx-react-lite";

export default observer(() => {
  return <AssetManagementIndex />;
});
