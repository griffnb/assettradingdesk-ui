#!/bin/bash
# Command Center Docker Sandbox Setup Script
#
# Runs inside the Docker sandbox after it starts.
# The workspace is mounted at the same path as your local working directory.
#
# Example: Install project dependencies
# cd "$CC_WORKSPACE_PATH"
# npm install

echo "Sandbox setup complete"
